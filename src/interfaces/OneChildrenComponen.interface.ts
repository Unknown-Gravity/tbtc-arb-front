import { ReactNode } from 'react';

/**
 * @name OneChildrenComponent
 *
 * @description This interface contains the props for a component that has only one children.
 */

export interface OneChildrenComponent {
	children: ReactNode;
}
